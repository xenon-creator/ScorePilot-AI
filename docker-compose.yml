version: '3.8'

services:
  # Relational Database Storage
  db:
    image: postgres:15-alpine
    container_name: aegis-postgres-db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: aegis_grading
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - aegis_network

  # Redis In-Memory Message Broker & Cache
  redis:
    image: redis:7-alpine
    container_name: aegis-redis-broker
    restart: always
    ports:
      - "6379:6379"
    networks:
      - aegis_network

  # API Gateway & Core NLP Engine REST Endpoint
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: aegis-fastapi-backend
    restart: always
    environment:
      - DB_HOST=db
      - DB_USER=postgres
      - DB_PASSWORD=postgres
      - DB_NAME=aegis_grading
      - REDIS_URL=redis://redis:6379/0
      - JWT_SECRET=super-secret-aegis-grading-key-2026
    ports:
      - "8000:8000"
    depends_on:
      - db
      - redis
    networks:
      - aegis_network

  # Asynchronous Background Worker Queue
  celery_worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: aegis-celery-worker
    command: celery -A app.workers.tasks.celery_app worker --loglevel=info
    restart: always
    environment:
      - DB_HOST=db
      - DB_USER=postgres
      - DB_PASSWORD=postgres
      - DB_NAME=aegis_grading
      - REDIS_URL=redis://redis:6379/0
    depends_on:
      - redis
      - db
    networks:
      - aegis_network

  # ScorePilot AI Next.js Frontend
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: aegis-nextjs-frontend
    restart: always
    ports:
      - "80:3000"
    depends_on:
      - backend
    networks:
      - aegis_network

volumes:
  postgres_data:

networks:
  aegis_network:
    driver: bridge
